interface:
  display_name: "Typos Spell Review"
  short_description: "Conservative typo triage before applying fixes"
  default_prompt: "Use $typos to scan this repo, export a review file with reasons and .typos.toml suggestions, then apply only approved spelling fixes."
